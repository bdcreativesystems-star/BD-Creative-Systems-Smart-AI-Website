# BD Smart AI (Frontend)

A 2026-style futuristic AI assistant UI (HTML/CSS/JS) that connects to a FastAPI backend.

## Run locally
Open `index.html` in a browser.

## Configure backend URL
Edit `app.js` and set:

```js
const BACKEND_URL = "https://YOUR-BACKEND-ON-RENDER.onrender.com";
